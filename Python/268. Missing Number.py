class Solution(object):
    def missingNumber(self, nums):
        a=len(nums)
        z=list(range())
        for i in range(len(nums)):
            if i in nums:
                pass
            else:
                a=i
        return a
        